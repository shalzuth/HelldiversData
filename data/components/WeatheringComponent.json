{ "WeatheringComponent" : {
    "tear_speed" : { "type": "float", "default": 0.05, "description": "How much tear is added per unit (i.e. 1 health = ? tear." },
    "dirt_speed" : { "type": "float", "default": 0.05, "description": "How much dirt is added per unit (i.e. 1 footstep = ? dirt." },
    "allow_dirt_falloff" : { "type": "byte", "description": "[bool]Should dirt falloff if this hasn't gotten dirtier in a while?" },
    "dirt_removal_time" : { "type": "float", "default": 180, "description": "Time it takes to go from completely covered in dirt to not covered in dirt." },
    "dirt_removal_cooldown" : { "type": "float", "default": 5, "description": "Start removing dirt from the avatar after X time has passed without getting dirtier." },
    "blood_application_speed" : { "type": "float", "default": 0.15, "description": "How much blood is added each splatter" },
    "scenario_application_speed" : { "type": "float", "default": 0.15, "description": "How much scenario stuff is added each splatter" },
    "height_wetness_application_speed" : { "type": "float", "default": 1, "description": "How much height wetness is added when in water" },
    "height_wetness_dirt_clean_speed" : { "type": "float", "default": 0.15, "description": "How fast the wetness cleans away dirt" },
    "height_wetness_scenario_clean_speed" : { "type": "float", "default": 0.05, "description": "How fast the wetness cleans away scenario stuff" },
    "height_wetness_blood_clean_speed" : { "type": "float", "default": 0.1, "description": "How fast the wetness cleans away blood" },
    "height_wetness_mud_clean_speed" : { "type": "float", "default": 0.08, "description": "How fast the wetness cleans away mud" },
    "global_wetness_speed" : { "type": "float", "default": 1, "description": "How fast the global wetness is applied" },
    "global_wetness_dirt_clean_speed" : { "type": "float", "default": 0.6, "description": "How fast the global wetness cleans away dirt" },
    "global_wetness_scenario_clean_speed" : { "type": "float", "default": 0.25, "description": "How fast the global wetness cleans away scenario stuff" },
    "global_wetness_blood_clean_speed" : { "type": "float", "default": 0.45, "description": "How fast the global wetness cleans away blood" },
    "global_wetness_dry_speed" : { "type": "float", "default": 0.01, "description": "How fast the global wetness dries up" },
    "height_wetness_dry_speed" : { "type": "float", "default": 0.005, "description": "How fast the height wetness dries up" },
    "scorch_marks_fade_away_speed" : { "type": "float", "default": 0.01, "description": "How fast the scroch marks dissapear" },
    "scorch_marks_add_speed" : { "type": "float", "default": 0.01, "description": "How fast the scroch marks are added" },
    "cape_tear_from_fire_speed" : { "type": "float", "default": 0.1, "description": "How fast the cape burns up" },
    "height_mud_application_speed" : { "type": "float", "default": 10, "description": "How much height mud is added when in mud" },
    "height_snow_application_speed" : { "type": "float", "default": 10, "description": "How much height mud is added when in mud" },
    "unfrost_speed" : { "type": "float", "default": 0.04, "description": "How fast frosting fades out" },
    "update_threshold" : { "type": "float", "default": 0.005, "description": "How much a contiguously updating value has to change before we refresh the material parameters." }
}}
