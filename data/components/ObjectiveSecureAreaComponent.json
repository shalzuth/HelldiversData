{ "ObjectiveSecureAreaComponent" : {
    "secure_area_radius" : { "type": "float", "default": 25, "description": "Radius around this objective marker which needs to be clear of areas to complete SecureArea objective stage" },
    "secure_rate" : { "type": "float", "default": 0.1, "description": "The rate of progress per second while securing the area. The area is considered secure when progress reaches 1.0" },
    "lost_rate" : { "type": "float", "default": -0.2, "description": "The rate of regress per second while the area is abandoned. The area is considered secure when progress reaches 1.0" },
    "center_on_objective_marker" : { "type": "byte", "description": "[bool]If true, then the central point for the area is the objective marker unit rather than the attached unit" },
    "animation_variable" : { "type": "uint", "description": "[string]Name of the animation variable to control with the progress value" },
    "animation_value_min" : { "type": "float", "default": 0, "description": "The minimum value for the range set to the animation variable" },
    "animation_value_max" : { "type": "float", "default": 1, "description": "The maximum value for the range set to the animation variable" },
    "noise_template" : { "type": "NoiseTemplate", "default": "NoiseTemplate_Objective_Flag" },
    "noise_time" : { "type": "float", "default": 2 },
    "encounter_refresh_time" : { "type": "float", "default": 10 },
    "encounter_start_percent" : { "type": "float", "default": 0.1, "description": "At which point at the securing progress do we trigger an encounter?" },
    "start_enabled" : { "type": "byte" },
    "loc_securing" : { "type": "uint", "default": 2313924635, "description": "[string]" },
    "loc_contested" : { "type": "uint", "default": 2812025640, "description": "[string]" },
    "loc_waiting" : { "type": "uint", "default": 2540642156, "description": "[string]" },
    "play_vo" : { "type": "byte" },
    "spawn_encounter" : { "type": "byte" },
    "skip_progress_step" : { "type": "byte" }
}}
